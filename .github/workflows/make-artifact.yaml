name: Make artifact

on:
  push:
    branches: [main, feat]

jobs:
  upload-artifact:
    runs-on: ubuntu-latest

    steps:
      - name: upload
        uses: actions/upload-artifact@v4
        with: 
         name: fajlovi
         path: .